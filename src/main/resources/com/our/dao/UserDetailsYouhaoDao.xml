<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.youhao.dao.UserDetailsYouhaoDao">
	<!-- 配置查询结果集和DO对象的映射关系 -->
	<!-- <resultMap type="DepartmentInfoDO" id="departmentmap">
		<id column="department_id" property="departmentId"/>结果集中查询的department_id字段会被封装到DepartmentInfoDO对象的departmentId属性
		<result column="department_name" property="departmentName"/>
	</resultMap> -->
	
	<select id="getUserDetailsList"  resultType="com.our.pojo.User_details">
		select * from user_details
	</select>
	
	<!-- <update id="UpdateUserDetails" parameterType="int">
		update user_details set icon=#{icon} set role=#{role} where 
	</update> -->
</mapper>